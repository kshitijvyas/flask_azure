# Environment: development or production
FLASK_ENV=development

# Development Database (Docker container on port 5433)
DEV_DATABASE_URL=postgresql://username:password@localhost:5433/database_name

# Production Database (used when FLASK_ENV=production)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Production Secret Key (IMPORTANT: Generate using: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with actual credentials
# 3. Never commit .env to Git!
